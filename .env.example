# Copy to .env and fill in. .env is gitignored — never commit real values.

# Sepolia RPC endpoint (Alchemy, Infura, or a public one).
SEPOLIA_RPC_URL=

# Testnet-only EOA private key for the NameGate issuer account. This account
# needs Sepolia ETH for gas. It is the account that holds ROLE_REGISTRAR on
# the UserRegistry and controls the compliance resolver.
# TESTNET KEY ONLY. Never put a real/mainnet key in this file.
ISSUER_PRIVATE_KEY=

# The namegate.eth token ID / labelhash context — filled in once you've
# registered namegate.eth yourself (see docs/BUILD-PLAN.md Day 1, Gate A).
# Leave blank until you have it; 01-setup-namespace.ts computes the labelhash
# for you and doesn't need this, but it's here for scripts that want to
# sanity-check against ETHRegistry.getExpiry().
NAMEGATE_LABEL=namegate

# Hedera testnet (Gate B). All Hedera scripts go through Hashio's EVM
# JSON-RPC relay via viem, so only the ECDSA hex private key is needed —
# not the native 0.0.x operator ID (Hedera's own SDK format), which nothing
# in this repo uses.
# Must be ECDSA hex (0x...), not Hedera Portal's DER-encoded default export.
HEDERA_OPERATOR_KEY=
HEDERA_RPC_URL=https://testnet.hashio.io/api
